# Docker compose defaults / overrides
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ia_crm
POSTGRES_PORT=5432

APP_DB_USER=ia_app
APP_DB_PASSWORD=ia_app

REDIS_PORT=6379

API_PORT=8000

# Auth
JWT_SECRET=dev-jwt-secret-change-me
JWT_ALG=HS256
ACCESS_TOKEN_TTL_SECONDS=3600
# Optional: if set, required for POST /auth/bootstrap
BOOTSTRAP_TOKEN=

# Optional overrides (normally you can leave these blank to use defaults)
# DATABASE_URL=postgresql+asyncpg://ia_app:ia_app@postgres:5432/ia_crm
# REDIS_URL=redis://redis:6379/0
# ENVIRONMENT=local
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
